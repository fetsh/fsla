<!DOCTYPE html>
<html>
<head>
  <title>: <%= @title %> :</title>
  <%= javascript_include_tag :defaults %>
  <%= render 'layouts/stylesheets' %>
  <%= csrf_meta_tag %>
</head>
<body>

    <div class="container">
      <%= render 'layouts/header' %>
      <section class='round'>
        <%= yield %>
      </section>
      <%= render 'layouts/footer' %>
    </div>

</body>
</html>
